#include "main.h"

/**
 * puts_half - prints half 
 * @str: string
 */

void puts_half(char *str)
{
	int length = 0;
	int i;
	int start;

	while (str[lenght] != '\0')
	{
		lenghth++
	}
	
	if (lenght % 2 == 0)
	{
		start = length /2 ;
	}
	else
	{
		start = (length - 1) / 2;
	}

	while (str[start] != '\0')
	{
		putchar(str[i]);
	}

	putchar('\n');
}
